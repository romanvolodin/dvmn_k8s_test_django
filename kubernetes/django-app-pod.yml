apiVersion: v1

kind: Pod
metadata:
  name: django-app
  labels:
    app.kubernetes.io/name: django-app
spec:
  containers:
    - name: django-app-container
      image: django_app
      imagePullPolicy: Never
